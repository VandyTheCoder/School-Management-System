= simple_form_for(@course) do |f|
  = f.error_notification
  .row
    .col-xs-12.col-md-6

      = f.input :name
      = f.input :description
      = f.input :grade_id, collection: Grade.all

  = f.button :submit, class: 'btn btn-primary btn-fill'
